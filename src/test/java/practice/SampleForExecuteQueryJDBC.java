package practice;

import com.mysql.jdbc.Driver;
import java.sql.DriverManager;
import java.sql.SQLException;

public class SampleForExecuteQueryJDBC 
{
	public static void main(String[] args) throws Throwable 
	{
		/*
		Driver driver=new Driver();
		//Step 1: Register the driver
		DriverManager.registerDriver(null);
		//Step 2:Get connection with database
		String nameOfDatabase="empinfo";
		Connection con=DriverManager.getConnection("jdbc:mysql://localhost:3306/"+nameOfDatabase+,"root","root");
		//Step 3: Issue create statement
		Statement state=con.createStatement();
		
		//Step 4: Execute any query
		RresultSet result = state.execurteQuery("select * from tableName;");
		
		while(result.next())
		{
			String value=result.getString();
			System.out.println(value);
		}
		
		//Step 5:Close the database
		con.close();
		
		*/
	}
}

